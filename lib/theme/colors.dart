import 'package:flutter/material.dart';

// Primary colors
const Color primaryBlue = Color.fromARGB(255, 0, 49, 99);
const Color primaryLight = Color(0xFF4D7AB8); // Lighter shade of primary
const Color primaryDark = Color(0xFF002B5C); // Darker shade of primary

// Background colors
const Color backgroundBlue = Color(0xFFCAE5FF);
const Color backgroundWhite = Color(0xFFF2F8FF);
const Color backgroundDark = Color(0xFF121212); // For dark mode

// Accent colors
const Color accentGreen = Color(0xFF4CAF50);
const Color accentRed = Color(0xFFE53935);
const Color accentYellow = Color(0xFFFFD600);

// Text colors
const Color textPrimary = Color(0xFF212121);
const Color textSecondary = Color(0xFF757575);
const Color textLight = Color(0xFFFFFFFF);
const Color textHint = Color(0xFFBDBDBD);

// Status colors
const Color success = Color(0xFF4CAF50);
const Color error = Color(0xFFE53935);
const Color warning = Color(0xFFFF9800);
const Color info = Color(0xFF2196F3);

// Card colors
const Color cardBackground = Color(0xFFFFFFFF);
const Color cardBackgroundDark = Color(0xFF1E1E1E);

// Gradient colors
const List<Color> primaryGradient = [primaryLight, primaryBlue, primaryDark];
const List<Color> successGradient = [Color(0xFF66BB6A), Color(0xFF388E3C)];
const List<Color> errorGradient = [Color(0xFFEF5350), Color(0xFFC62828)];
